#include<stdio.h>
#include<string.h>
int main()
{
	char str[100];
	char str1[100];
	gets(str);
	gets(str1);
	int len=strlen(str);
	int len1=strlen(str1);
	int arr[10];
	int ar1[10];
	int i,j;
	for(i=0;i<26;i++)
	{
		arr[i]=0;
		arr1[i]=0;
	}
	if(len==len1)
	{
		for(i=0;i<len;i++)
		{
			
		}
	}
}
